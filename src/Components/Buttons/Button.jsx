import { Menu, X } from "lucide-react";

const HireMeButton = () => {
  return (
    <button className="absolute text-primary top-7.5 right-4 hover:text-muted">
      <X size={24} />
    </button>
  )
}

export default HireMeButton;